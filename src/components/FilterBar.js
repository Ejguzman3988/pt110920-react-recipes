import React from 'react'

function FilterBar() {
    return (
        <div id="filter-bar">
            <label htmlFor="filter">Search by Recipe Title: </label>
            <input type="text"/>
        </div>
    )
}

export default FilterBar
